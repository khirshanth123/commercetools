/* eslint-disable no-unused-vars */
export enum BREAKPOINT {
    XS = 'xs',
    SM = 'sm',
    MD = 'md',
    LG = 'lg',
    XL = 'xl',
    XXL = '2xl',
  }
  
  export enum ScreenSize {
    MOBILE = 'mobile',
    TABLET = 'tablet',
    DESKTOP = 'desktop',
    WIDESCREEN = 'widescreen',
  }
  
  export enum BannerType {
    IMAGE = 'image',
    HERO = 'hero',
    SPLIT_VIEW = 'split-view',
  }
  
  export enum BannerPositionX {
    LEFT = 'left',
    RIGHT = 'right',
  }
  
  export enum BannerPositionY {
    TOP = 'top',
    BOTTOM = 'bottom',
  }
  
  export enum ImageFormat {
    ZOOM = 'zoom',
    PRODUCT = 'product',
    THUMBNAIL = 'thumbnail',
    STORE = 'store',
    CART_ICON = 'cartIcon',
  }
  